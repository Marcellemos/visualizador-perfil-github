# visualizador-perfil-github

Endpoints que vamos usar

Perfil: GET /users/:username

Repositórios: GET /users/:username/repos (usar query per_page=10 e sort=update / sort=pushed conforme objetivo )

